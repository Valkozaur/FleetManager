# Production Environment Configuration
# Copy this file to .env.production and fill in your production values

# =============================================================================
# Gmail Configuration
# =============================================================================
GMAIL_CREDENTIALS_FILE=/app/credentials/credentials.json
GMAIL_CHECK_INTERVAL=300

# =============================================================================
# Google Gemini AI Configuration
# Required for email classification and logistics extraction
# Get your API key: https://makersuite.google.com/app/apikey
# =============================================================================
GOOGLE_GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# Google Sheets Configuration
# Required for saving processed orders to Google Sheets
# Get spreadsheet ID from the URL: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/...
# =============================================================================
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id-here

# =============================================================================
# Google Maps Configuration (Optional)
# Required for geocoding addresses to coordinates
# Get API key: https://console.cloud.google.com/apis/credentials
# Enable: Geocoding API, Maps JavaScript API
# =============================================================================
GOOGLE_MAPS_API_KEY=your-maps-api-key-here

# =============================================================================
# Application Configuration
# =============================================================================
LOG_LEVEL=INFO
TEST_MODE=false
DATA_DIR=/app/data

# =============================================================================
# Email Query Configuration (Optional)
# Customize the Gmail search query for fetching emails
# Examples:
#   - is:unread category:primary newer_than:7d
#   - from:supplier@example.com is:unread
#   - subject:"order confirmation" is:unread
# =============================================================================
# TEST_EMAIL_QUERY=is:unread category:primary newer_than:7d

# =============================================================================
# Docker Configuration (Used in docker-compose.prod.yml)
# =============================================================================
COMPOSE_PROJECT_NAME=fleetmanager
