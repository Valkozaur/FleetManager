version: '3.8'

services:
  # Email Processing Service
  email-processor:
    build:
      context: ./services/email-processor
      dockerfile: Dockerfile
    container_name: email-poller
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist data and tokens
      - email_poller_data:/app/data
      - email_poller_logs:/app/logs
      # Mount credentials (you'll need to provide these)
      - ./credentials:/app/credentials:ro
    environment:
      - DATA_DIR=/app/data
      - GMAIL_CREDENTIALS_FILE=/app/credentials/credentials.json
    mem_limit: 512m
    mem_reservation: 256m
    cpus: 1.0
    healthcheck:
      test: ["CMD-SHELL", "test -f /app/credentials/credentials.json || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - fleetmanager-network

  # Future Web UI Service
  # ui:
  #   build:
  #     context: ./services/ui
  #     dockerfile: Dockerfile
  #   container_name: fleetmanager-ui
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - REACT_APP_API_URL=http://api-gateway:8080
  #   depends_on:
  #     - api-gateway
  #   networks:
  #     - fleetmanager-network

  # Future API Gateway Service
  # api-gateway:
  #   build:
  #     context: ./services/api-gateway
  #     dockerfile: Dockerfile
  #   container_name: fleetmanager-api-gateway
  #   restart: unless-stopped
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - EMAIL_PROCESSOR_URL=http://email-processor:8000
  #   depends_on:
  #     - email-processor
  #   networks:
  #     - fleetmanager-network

  # Nginx Reverse Proxy (future)
  # nginx:
  #   image: nginx:alpine
  #   container_name: fleetmanager-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - ui
  #     - api-gateway
  #   networks:
  #     - fleetmanager-network

volumes:
  email_poller_data:
    driver: local
  email_poller_logs:
    driver: local

networks:
  fleetmanager-network:
    driver: bridge